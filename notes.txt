go to node.js https://nodejs.org/en/download
Scroll down below the thing that lets you select your OS, etc - there is a link to Windows installer (msi)

Abandoned the following WSL method (Found there is a node.js windows installer that doesn't need docker)
Installed node.js with Windows WSL Ubuntu from instructions:
https://nodejs.org/en/download

# Download and install nvm:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
# in lieu of restarting the shell
\. "$HOME/.nvm/nvm.sh"
# Download and install Node.js:
nvm install 24
# Verify the Node.js version:
node -v # Should print "v24.12.0".
# Verify npm version:
npm -v # Should print "11.6.2".

Installed Java jdk-17.0.12 in \Portable with "current" symbolic link and
  added JAVA_HOME and PATH to JAVA_HOME/bin
Installed Maven apache-maven-3.9.12 in \Portable with "current" symbolic
  link and added MAVEN_HOME and PATH to MAVEN_HOME\bin